# Copy from the repo root with:
#   cp tests/.env.example tests/.env
# tests/playwright.config.ts auto-loads tests/.env if it exists.

# Defaults to the current production web domain. Override for preview or local targets.
E2E_BASE_URL=https://ai-stem-learning-platform-group-8.vercel.app
E2E_TEACHER_EMAIL=
E2E_TEACHER_PASSWORD=
E2E_STUDENT_EMAIL=
E2E_STUDENT_PASSWORD=
E2E_JOIN_CODE=
